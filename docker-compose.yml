version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - /var/www/teekit-by-mirza:/app
    command: sh -c "php artisan key:generate && php artisan serve --host=0.0.0.0 --port=8000"

volumes:
  local-teekit:
    external: true
